const func = () => {
  // Refactor this to be its own function
  // Using "ctrl + . we can refactor our code"
  const randomPercentage = `${(Math.random() * 100).toFixed(2)}%`;
  console.log(randomPercentage);
};
